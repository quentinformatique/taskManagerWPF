<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:taskManagerWPF.Models">
    <DataTemplate x:Key="TaskTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="450" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <CheckBox IsChecked="{Binding done}" Grid.Column="0" Margin="0,10,0,10"/>
            <TextBlock Text="{Binding title}" Grid.Column="1" Margin="20,10,0,10"/>
            <Button Content="Delete" Grid.Column="2" HorizontalAlignment="Right" Margin="20,10,0,10"  Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" />
        </Grid>
    </DataTemplate>
</ResourceDictionary>
